# Data

Public, non-sensitive fixture data used by the engine and its tests.

- `meta.yaml`: document and blog registry for the public fixture set
- `*-data.yaml`: sample structured inputs for rendering
- `blog/`: sample blog metadata used by blog rendering tests
- `jobs/`: sample briefs for tailoring tests

Sensitive or client-owned datasets belong in `euxis-publisher-private`.
