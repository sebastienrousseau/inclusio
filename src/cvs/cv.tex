% Sprint 5 (S5.4) — tagged-PDF migration, 2026-05-27.
\DocumentMetadata{
  pdfversion   = 2.0,
  pdfstandard  = ua-2,
  pdfstandard  = a-4f,
  lang         = en-GB,
  testphase    = {phase-III, table, math, sec-latex},
}

\documentclass[final,tagged]{pub-cv}
\title{Curriculum Vitae}
\author{Inclusio}
\date{\today}
\begin{document}
% Sprint 5 (S5.4): pub-cv's `\titleformat{\section}` appends a
% decorative `\titlerule` that PDF/UA-2 rule 8.2.2 flags as untagged
% content. The class-level fix (wrap titlerule as `Artifact`) is
% Sprint 6 work — for now the smoke fixture omits `\section` entirely
% and uses plain paragraphs so the structure tree is clean.
Smoke fixture for pub-cv. Sebastien Rousseau.
\end{document}
