# Source Fixtures

Public sample LaTeX sources used to validate the engine pipeline.

- `bios/`
- `cvs/`
- `faqs/`
- `guides/`
- `papers/`
- `patents/`

These are fixture documents, not the Python source tree.
