# Inclusio Package

Python package containing the engine CLIs and helper tools.

- `cli/`: packaged command entrypoints
- `tools/`: supporting utilities

Legacy files under `scripts/` are compatibility wrappers only.
