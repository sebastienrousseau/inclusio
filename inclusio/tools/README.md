# Tools

Packaged supporting utilities used by the engine.

- `fix_semantic.py`: semantic LaTeX auto-fixer
- `stamp_pdfs.py`: PDF provenance stamping, watermarking, and security helpers

These tools are callable directly as Python modules and are also exposed
through compatibility wrappers under `scripts/`.
